import { createSandbox } from 'sinon'
import { expect } from 'chai'
import { SinonSandbox } from 'sinon'
import {  } from '../../utils/string.util'

describe('object utils', () => {
  let sandbox: SinonSandbox

  before(() => {
    sandbox = createSandbox()
  })

  beforeEach(() => {
  })

  afterEach(() => {
    sandbox.restore()
  })

  it('', () => {
    
  })
})
